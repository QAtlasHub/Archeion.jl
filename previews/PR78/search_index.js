var documenterSearchIndex = {"docs":
[{"category":"section","location":"spec/#The-format","page":"The format","text":"Below is SPEC.md from the repository, verbatim and generated into this page at build time. It is the normative document: Archeion.jl is one implementation of it, and a registry that satisfies it can be read without this package at all.","title":"The format"},{"category":"section","location":"spec/#Registry-format,-version-2","page":"The format","text":"This file is the format. Archeion.jl is one implementation of it and may be replaced; a registry is valid when it satisfies this document, whatever wrote it.\n\nStatus: stable. Sections 1-9 are normative for spec = \"registry/2\" and no longer change in ways that make a valid registry invalid. Section 10 lists what is not decided yet; nothing there may be relied on, and nothing there needs deciding for a registry to be used. Section 11 is how a registry/1 tree is converted, which is the only thing this version does with one.\n\nWhat may still be added to registry/2: an optional field, a new event kind, a new value of a field whose unknown values already have a defined reading (record.kind, event.kind, an observation's binding). What may not: a new required field, a changed meaning, a changed path rule.","title":"Registry format, version 2"},{"category":"section","location":"spec/#1.-Names-and-paths","page":"The format","text":"R1 Path segments under projects/ and records/ use only ASCII [A-Za-z0-9_.-]: no non-ASCII (NFC/NFD normalisation differs between macOS and Linux) and no spaces. No two entries of one directory are equal once lower-cased (a case-insensitive filesystem would merge them). Identifiers and slugs are lower case (R5, R6); upper case appears only in the fixed names of this document (README.md, SHA256SUMS) and in the T and Z of R2 times.\nR2 No : anywhere in a path. A time in a path is YYYYMMDDTHHMMSSZ, UTC, without separators. (Windows cannot check out a file whose name contains :.)\nR3 A path segment is at most 64 characters; a path relative to the repository root is at most 180 characters.\nR4 No segment is a Windows reserved name (con, prn, aux, nul, com1-com9, lpt1-lpt9, with or without an extension), and no segment ends in ..\nR5 An identifier is a UUID (RFC 4122), written in the usual 8-4-4-4-12 hexadecimal form in lower case. It is generated once, lives inside the file it identifies, and never appears in a path: 36 characters say nothing to a reader and would spend most of the R3 budget.\nR6 A path carries a slug instead: lower-case [a-z0-9] words joined by -. A slug is a name, not an identity — renaming one moves the directory and changes nothing else, and two registries may use the same slug for different things. A revision directory is named by its time and tag, which are already unique within a record.\nR7 Times inside files are RFC 3339 in UTC with a Z suffix. Paths use R2, files use R7.","title":"1. Names and paths"},{"category":"section","location":"spec/#2.-Objects-and-layout","page":"The format","text":"Object What it is Mutable?\nProject a line of research, with a display name display name only\nRecord one question, answered by a series of revisions never\nRevision one frozen answer: a rendered report and what it was made from never\nEvent a fact added later about a record or revision (comment, correction, yank, ...) never\n\n\"Latest\", indexes, search and dashboards are derived from these and are not committed (§9).\n\nregistry.toml               what the registry is, and its index (§2.1)\nprojects/<slug>.toml\nrecords/<YYYY>/<slug>/\n    record.toml\n    revisions/<YYYYMMDDTHHMMSSZ>-<tag>/\n        entry.toml\n        README.md\n        SHA256SUMS\n        gallery/            the human face (HTML, figures)\n        agent/              the machine face (agent.json, agent.md, figure data)\n        provenance.toml     per-point provenance: a summary (optional, §5.5)\n        provenance/         its point table\n        repro/              whatever was captured to rebuild it (optional)\n    events/<YYYYMMDDTHHMMSSZ>-<origin>-<key>.toml\n\nIdentity is a UUID inside the file; the path is a name a reader can use. This is how Julia's General registry is arranged (D/DataFrames/, with the UUID in Package.toml), for the same reason: a directory listing should say what is in it.\n<YYYY> is the record's creation year in UTC. A record's project is a field, not a path segment: a record can be reassigned without moving it.\n<tag> is four characters of lower-case Crockford base32 (0-9, a-z without i, l, o, u), so two revisions frozen in the same second differ.\nA slug is unique among its siblings, which R1 requires of any two entries of a directory anyway.\n_incoming/ (a deposit in progress) and _site/ (a build) are never committed.","title":"2. Objects and layout"},{"category":"section","location":"spec/#2.1-The-index","page":"The format","text":"registry.toml says what the registry is and carries a table of every project and record by UUID — the shape of General's [packages], one line each, sorted by UUID:\n\nspec = \"registry/2\"\nname = \"vault-registry\"\nuuid = \"6f1d8c5e-1a2b-4c3d-9e8f-0a1b2c3d4e5f\"\n\n[projects]\nc0ffee00-1111-4222-8333-444444444444 = { name = \"openboundary\", path = \"projects/openboundary.toml\" }\n\n[records]\ndeadbeef-5555-4666-8777-888888888888 = { name = \"Does the environment remove the open boundary?\", path = \"records/2026/open-boundary\" }\n\nThe index is derived, and committed anyway. Derived, because every line of it is already in the tree and a reader may rebuild it by walking projects/ and records/; committed, because a reader that only wants to resolve a UUID should not have to walk anything, and because a registry should say what it holds without a tool.\n\nThat it is derivable is what makes it cheap: a validator checks it against the tree and reports any disagreement, and a writer that meets a conflict in it — the one file every deposit touches — regenerates it instead of merging by hand.","title":"2.1 The index"},{"category":"section","location":"spec/#3.-projects/slug.toml","page":"The format","text":"Field Required Meaning\nspec yes \"registry/2\"\nuuid yes R5; generated once, never changed\nname yes display name; may change\ncreated yes R7 time\nmigrated no written by a conversion, never by hand: spec and id are what this file was under the older format, at is R7 time when it was converted (§11)","title":"3. projects/<slug>.toml"},{"category":"section","location":"spec/#4.-record.toml","page":"The format","text":"Field Required Meaning\nspec yes \"registry/2\"\nuuid yes R5; generated once, never changed\nkind yes \"report\" (a rendered result) or \"note\" (a lab note). A reader that does not know a kind shows the record as it is, and does not drop it\nproject yes the UUID of a project in projects/\ntitle yes what the record is called in the index; the current revision's title is what a reader sees\ncreated yes R7 time; its UTC year equals the directory's year\nmigrated no written by a conversion, never by hand: spec, id and project are what this record was under the older format, at is R7 time when it was converted (§11)\n\nA record file is written once. Anything that changes later is an event — or a conversion, which is the one thing that may rewrite it, and says so in migrated.","title":"4. record.toml"},{"category":"section","location":"spec/#5.-Revisions","page":"The format","text":"","title":"5. Revisions"},{"category":"section","location":"spec/#5.1-entry.toml","page":"The format","text":"The only structured file in a revision. It is written by the tool that holds the document model at render time; it is never reconstructed by parsing the rendered output.\n\nField Required Meaning\nspec yes \"registry/2\"\nid.project, id.record yes the UUIDs in record.toml and the project it names\nid.rev, id.kind yes must agree with the directory name and record.toml\nparents yes revision ids of this record this revision revises; [] for the first\ntime.frozen yes when the revision was frozen; equals the directory's time to the second\ntime.rendered no when the report was rendered\ndoc.title yes the report's title\ndoc.status yes \"trial\" or \"final\": how far the author vouches for it (§5.4)\ndoc.question, doc.claim no one sentence each: what was asked, what can be said\ndoc.tags no list of strings\nanchors.stable yes ids that keep their meaning across revisions and may be commented on\nanchors.local yes ids valid inside this revision only (auto-numbered)\nsource no where the code came from (§5.3)\npreservation.level yes \"read\"; higher levels are established by events (§6)\npreservation.external no URLs the revision needs to display fully\nmigrated no written only by a conversion from archeion/0.3, which predates this format and rewrote revisions. A registry/1 → registry/2 conversion never writes it: it may not open a revision at all, and says what the record was on the record instead (§4, §11)\n\nUnknown fields are ignored by readers. A later registry/2 never adds a required field; that needs registry/3, and readers keep reading the versions before it.\n\nA field whose value is not known is omitted. An empty list means \"known to be empty\", never \"unknown\". A byte count is a positive integer. A commit is 40 hexadecimal characters. A digest is \"sha256:\" followed by 64 hexadecimal characters.","title":"5.1 entry.toml"},{"category":"section","location":"spec/#5.2-SHA256SUMS-and-README.md","page":"The format","text":"SHA256SUMS lists every file of the revision except itself, one per line, in the format of sha256sum: 64 hex, two spaces, the path relative to the revision directory. It is written last; a revision without it is incomplete. sha256sum -c SHA256SUMS checks a revision without any tool from this repository.\nREADME.md is a plain-text rendering of entry.toml, written once with it, so that a revision can be read with nothing but a text viewer.","title":"5.2 SHA256SUMS and README.md"},{"category":"section","location":"spec/#5.3-Where-the-code-came-from","page":"The format","text":"source records the repositories whose code produced the revision:\n\n[source]\ncaptured = \"publish\"      # when the state was read: \"run-start\", \"completion\" or \"publish\"\n\n[[source.repo]]\nrole = \"render\"           # \"compute\", \"analysis\" or \"render\"\ncommit = \"<40 hex>\"\ndirty = false\n\ncaptured says when the repository's working tree was observed, and nothing more. None of its values says which code a process had loaded: a process can run code it loaded before the observation, or code that is not on disk at all (a function defined in a REPL, a closure sent from another process). \"run-start\", \"completion\" and \"publish\" are three moments of observation, not a scale of strength, and source never claims that the observed commit produced the result. That claim, and the provenance of individual parameter points (which point was computed by which code, and which bytes the report read), is §5.5.","title":"5.3 Where the code came from"},{"category":"section","location":"spec/#5.4-doc.status","page":"The format","text":"\"final\" means the author presents this revision's claims, at the time of freezing, as correct to a third party. \"trial\" means everything else. Withdrawal is not a status; it is an event.","title":"5.4 doc.status"},{"category":"section","location":"spec/#5.5-Per-point-provenance","page":"The format","text":"A revision may hold provenance.toml, which says for each parameter point the report used which bytes it read and what the computation recorded when it wrote them. Its point table is a separate file, provenance/points.tsv, because a report can use tens of thousands of points:\n\nkey\tfile\tread_sha256\tresult_sha256\tobservation\tcompleted_at\n\nOne row per point, sorted by key, keys unique; cells are escaped with Julia's escape_string, so none holds a tab or a newline. read_sha256 is the digest of the bytes the report read (read once, hashed, then loaded from the same copy); result_sha256 is what the computation recorded when it wrote the file; observation is the token of the source observation the computing process made. A value that was not recorded is unknown.\nprovenance.toml:\n\nField Meaning\nschema \"registry.provenance/1\"\npoints, points_file, points_digest the row count, \"provenance/points.tsv\", and \"sha256:<hex>\" of that file\ncounts read_matches_result, read_differs_from_result, result_unknown: the rows by how read_sha256 compares with result_sha256\nbindings the rows by the binding of the observation they name; unknown for a row whose observation is unknown or absent\nobservations, missing_observations the tokens the rows (and the render) name that are, and are not, held in the revision\nrender_observation the token of the rendering process's own observation, when it made one\nallow_mismatch, source_contents whether rows that read other bytes than were recorded were let in, and whether source contents were copied\n\nEach observation is held at repro/observations/<token>.toml, as the data store wrote it. Its source names a snapshot src1-<hex>, where <hex> is the SHA-256 of the snapshot's files.tsv (an inventory of the source roots the process could see); the snapshot is held at repro/sources/<first 32 of hex>/{files.tsv,state.toml}, and the contents of inventoried files, when kept, at repro/blobs/<first 32 of the file's SHA-256>. Paths use 32 hex (128 bits) to stay within R3; the files keep the full digests, and a checker compares full digests.\nAn observation's binding is how far that process's loaded code was checked against its snapshot: loaded-differs-from-disk (a loaded package's sources are not what the snapshot holds) or unverified (with binding_reasons). An observation is a disk state, and unverified claims nothing about the code that ran. A binding a reader does not know is read as unverified, so a data store may add one (a process launched from the snapshot, §10) without a new spec version. A loaded-matches-disk, which earlier data stores wrote, is read and counted as unverified: a match cannot be shown from inside the computing process, since code defined outside a package leaves no trace to check it against.\nA depositor refuses a row whose read_sha256 differs from its result_sha256 unless told to let it in, which is then recorded as allow_mismatch = true. A validator requires the summary to match the table, every named token to be held or listed as missing, every binding to be one of the three values, and every snapshot to hash to its id; it warns on rows that read other bytes, on missing observations, and on every loaded-matches-disk.\nWhen provenance.toml exists, repro/observations/, repro/sources/ and repro/blobs/ are its own.\nWhat a recomputation reads from an observation, when present (all optional; a reader that finds none of it can still show the revision): its roots' kind — git or plain for the study and packages developed beside it, depot for a package loaded from a depot, whose head is the tree the Manifest pins, and artifact for an artifact such a package loaded, named artifact:<name>:<tree>; program, the script the process ran; and under julia, bindir and executable_sha256 (which binary), platform, and blas_threads. A symlink's blob, when kept, holds its target. A root's files are laid out at the path its kind names: a depot package at packages/<name>/<slug> and an artifact at artifacts/<tree> of a depot, and checked against that tree.","title":"5.5 Per-point provenance"},{"category":"section","location":"spec/#6.-Preservation","page":"The format","text":"A revision is read when its gallery/ opens in a browser from the repository alone, apart from the URLs listed in preservation.external. render (rebuilt from what the revision holds) and compute (recomputed from preserved data) are claims that must be earned: an event of kind capability.verified records when, where and how the rebuild succeeded. Until then a revision is read, whatever files it carries.\n\nA capability.verified event may say what was earned and how (all optional): capability (\"compute\" or \"render\"), criterion (for example \"result-file-sha256\": every point's result file, recomputed, hashed as its row's result_sha256), points and matched, the observation it recomputed from, and conditions — depot (\"restored-only\": nothing but what the revision holds), home, network (\"unshared\" when the process had no network namespace, \"not-blocked\" when only offline flags were set, which does not show the network was unused), host, julia_version, executable_sha256, threads, blas_threads. A reader shows these as given; an event that states none of them claims only what its kind says.","title":"6. Preservation"},{"category":"section","location":"spec/#7.-Events","page":"The format","text":"An event file is named <YYYYMMDDTHHMMSSZ>-<origin>-<key>.toml. origin is loc for an event written here, with key four random characters of the R5 alphabet, or the name of an external source, with key derived from that source's own identifier so that importing the same thing twice produces the same file name.\nRequired fields: spec, kind, at (R7), subject.record (the containing record's UUID). subject.rev and subject.anchor narrow the subject.\nKinds in registry/2: comment, yank, supersede, capability.verified. A reader counts and shows events of a kind it does not know; it does not drop them.\nAn event is never edited. An edit elsewhere becomes a new event.","title":"7. Events"},{"category":"section","location":"spec/#7.1-Which-revision-is-current","page":"The format","text":"live   = revisions not named by a yank event\nheads  = live revisions that are not a parent of another live revision\n         and are not the target of a supersede event from a live revision\n|heads| = 1  → that revision is current\n|heads| = 0  → the record is withdrawn\n|heads| > 1  → the record is in conflict; nothing is chosen by time","title":"7.1 Which revision is current"},{"category":"section","location":"spec/#8.-What-a-revision-must-not-say","page":"The format","text":"A revision states what it used. It does not state what has been computed: that is the data store's current state, not a property of a frozen answer. entry.toml contains no key named completed, available, missing, done or exists.","title":"8. What a revision must not say"},{"category":"section","location":"spec/#9.-Committed-and-derived","page":"The format","text":"Committed: every object of §2, and files derived from exactly one revision and frozen with it (README.md, SHA256SUMS). Derived and never committed: anything computed from several objects — the catalogue, search indexes, the current revision of a record, counts.","title":"9. Committed and derived"},{"category":"section","location":"spec/#10.-Not-decided-(not-normative)","page":"The format","text":"Starting processes from a snapshot. A fourth binding, launched-from-snapshot, for a process whose code was loaded from a held snapshot rather than checked against one (§5.5).\nSource capture for a dirty tree. A git commit object built from a temporary index, or a content-addressed tar of declared source roots.\nData references and replicas. Content digests of the data a revision used, and events that record where copies are and when they were last verified.\nComments from GitHub. Import by a full, idempotent scan; the key of an imported event.\nPublic copies. A public revision generated afresh from a public profile, with the private correspondence recorded on the private side only.","title":"10. Not decided (not normative)"},{"category":"section","location":"spec/#11.-Migrating-from-registry/1","page":"The format","text":"registry/1 trees are converted, not read alongside: a tool that had to understand both would carry two of everything for the sake of a version that no registry stays on. registry.toml's spec says which version a tree is, and a converter rewrites it in one step. The differences are only those of §§1-4:\n\n registry/1 registry/2\nidentity p_/r_ and eight Crockford characters a UUID (R5)\nwhere identity lives in the path and in the file in the file only\nproject file projects/<id>.toml projects/<slug>.toml\nrecord directory records/<YYYY>/<date>-<slug>-<id>/ records/<YYYY>/<slug>/\nindex none registry.toml (§2.1)\n\nRevisions, events, provenance and preservation are unchanged, so converting is renaming and rewriting four fields: each record directory loses its date and identifier, each project file is named by its slug, id becomes uuid everywhere it appears, and the index is generated. A revision's own files are never written, SHA256SUMS included; the conversion only reads one, to recover the title registry/1 did not store on the record. A tree is never a mixture: the spec of a registry is the spec of every file in it.\n\nThat last sentence has one exception, and it is bounded. A revision frozen under registry/1 says so in its own entry.toml, and names the identifiers of that day — and it may not be rewritten, because its SHA256SUMS covers that file (§5.2). So a converted project or record keeps a migrated table saying what it was, and a reader accepts those older values in a revision or event dated before migrated.at, and nowhere else. Anything frozen after the conversion is registry/2 like the rest of the tree.\n\nA conversion cannot finish by itself. Whatever names a record from outside the registry — a binding in the repository that renders the report (§8) — still holds the identifier of the older format, and the registry cannot reach it. So a converter reports which identifier became which UUID, and those names are updated where they live. A deposit through a binding that still names an old identifier is refused, not quietly turned into a second record.\n\nA conversion is all or nothing. A half-converted tree is neither version, so a converter settles every identifier, slug and collision before it renames anything, and a registry under version control is converted only from a clean working tree — so that what a failure leaves behind is one git checkout from what was there before.","title":"11. Migrating from registry/1"},{"category":"section","location":"api/#API-References","page":"API References","text":"What a caller needs, grouped by when they need it. Everything else in the module is an internal detail and may move between patch releases.\n\nTwo names are exported — deposit and new_binding — because they are the two a study script calls. The rest are reached as Archeion.…, which keeps a registry script explicit about what it is doing.","title":"API References"},{"category":"section","location":"api/#Starting-a-registry","page":"API References","text":"","title":"Starting a registry"},{"category":"section","location":"api/#Putting-a-result-in","page":"API References","text":"","title":"Putting a result in"},{"category":"section","location":"api/#Checking-and-reading","page":"API References","text":"","title":"Checking and reading"},{"category":"section","location":"api/#Talking-to-the-remote","page":"API References","text":"","title":"Talking to the remote"},{"category":"section","location":"api/#Converting-a-registry/1-tree","page":"API References","text":"","title":"Converting a registry/1 tree"},{"category":"section","location":"api/#Provenance","page":"API References","text":"","title":"Provenance"},{"category":"section","location":"api/#Recomputing-a-revision-from-itself","page":"API References","text":"restore lays out what a revision's repro/ holds; verify also runs the study there, with nothing else to draw on, and writes capability.verified only when every point's result comes out the same (SPEC §6).","title":"Recomputing a revision from itself"},{"category":"section","location":"api/#The-command-line","page":"API References","text":"","title":"The command line"},{"category":"section","location":"api/#The-module","page":"API References","text":"","title":"The module"},{"category":"function","location":"api/#Archeion.init","page":"API References","text":"init(root; name = basename(root), title = name, tagline = \"\", pages = true, kw...) -> (; written, skipped)\n\nStart a registry at root: the directories a registry needs, registry.toml with its [site] banner, a .gitignore for what is derived, and — unless pages = false — the workflows that check it and publish its catalogue (kw... reaches setup_pages: branch, runner, site). Returns the paths written, relative to root. Refuses a root that already holds a registry rather than writing over what is there.\n\n\n\n\n\n","title":"Archeion.init"},{"category":"function","location":"api/#Archeion.setup_pages","page":"API References","text":"setup_pages(root; version = this version, branch = default_branch(root), validate = true,\n            runner = \"ubuntu-latest\", site = nothing) -> (; written, skipped)\n\nWrite the workflows that publish root as a site and check it on every push. Returns the paths written and those skipped, relative to root. The Archeion version is pinned to the one writing them, which is what registry.toml should name.\n\nBy default the site is GitHub Pages. A private repository's Pages site is public on every plan but Enterprise Cloud, so for a registry that must not be, pass site — a directory on the machine runner names, built into on every push and read from there over SSH — and the Pages workflow is not written at all.\n\n\n\n\n\n","title":"Archeion.setup_pages"},{"category":"function","location":"api/#Archeion.default_branch","page":"API References","text":"default_branch(root) -> String\n\nThe branch the workflows should watch: the one root is on, or what its remote calls default. Asked rather than assumed — GitHub has created repositories on main since 2020, and a workflow whose on: push: branches: names a branch that does not exist never runs and never says so.\n\n\n\n\n\n","title":"Archeion.default_branch"},{"category":"function","location":"api/#Archeion.new_binding","page":"API References","text":"new_binding(path; root, project, slug, kind = \"report\") -> (; record)\n\nChoose a new record identifier and write the binding file at path. Refuses if path exists: a binding is created once and committed, and every later deposit through it adds a revision to the same record. root is the registry, stored relative to the binding file's directory. kind is what the record holds — \"report\" for a rendered result, \"note\" for a lab note — and is fixed with the record, because a record answers one question in one way (§4).\n\n\n\n\n\n","title":"Archeion.new_binding"},{"category":"function","location":"api/#Archeion.registry_of","page":"API References","text":"The registry a binding names, as an absolute path: the binding stores it relative to itself.\n\n\n\n\n\n","title":"Archeion.registry_of"},{"category":"function","location":"api/#Archeion.deposit","page":"API References","text":"deposit(binding; gallery, agent, doc, source_repo, external = [], repro = Dict(),\n        parents = nothing, push = true, provenance = nothing) -> NamedTuple\n\nFreeze a new revision of the binding's record: copy gallery and agent, write entry.toml, README.md and SHA256SUMS, move it into place, validate the whole registry (and take the revision back out if that fails), then commit only that path and push.\n\ndoc carries what the document model knows: title, status (\"trial\"/\"final\"), anchors split into stable and positional (written as anchors.local), and optionally tags, question, claim. parents defaults to the record's current revision; a record in conflict needs them named. repro maps paths under repro/ to files.\n\nprovenance adds per-point provenance (SPEC.md §5.5): the keywords of write_provenance!, which provenance_from builds from a DataVault vault and a Pinax.report result. A point whose bytes read differ from what its computation recorded makes the deposit refuse, unless allow_mismatch = true.\n\n\n\n\n\n","title":"Archeion.deposit"},{"category":"function","location":"api/#Archeion.publish","page":"API References","text":"publish(vault, recipe; binding, title, out, status, source_repo, remote = :pr, ...) -> NamedTuple\n\nRender a vault through recipe, deposit both faces as a new revision of the binding's record with the table of what was read, and send that commit to the shared registry. The one call a study makes; the method is defined when both Pinax and DataVault are loaded.\n\nstatus (:trial or :final) is required: what a revision vouches for is the author's to state (SPEC §5.4), not a default to inherit. remote is :pr (a branch and a pull request), :push (straight onto the current branch, rebasing once if the remote moved) or :local (commit only). Before anything is written the registry clone is brought to its remote, and the commit that rendered the report is checked for being published — a revision cites it.\n\n\n\n\n\n","title":"Archeion.publish"},{"category":"function","location":"api/#Archeion.doc_fields","page":"API References","text":"doc_fields(doc; tags = String[], question = nothing, claim = nothing) -> NamedTuple\n\nWhat deposit needs from a document model: title, status, stable and positional anchors, and the optional fields given. The method for a Pinax.Document is defined when Pinax is loaded, and reads the document that was rendered, never its output.\n\n\n\n\n\n","title":"Archeion.doc_fields"},{"category":"function","location":"api/#Archeion.anchors","page":"API References","text":"anchors(ids; auto = r\"_(fig|tbl)\\d+$\") -> (; stable, positional)\n\nSplit anchor ids into those that keep their meaning across revisions and those Pinax numbered by position (<section>_fig<N>, <section>_tbl<N>), which point elsewhere once a figure is inserted.\n\n\n\n\n\n","title":"Archeion.anchors"},{"category":"function","location":"api/#Archeion.validate","page":"API References","text":"validate(root) -> (; errors, warnings, summary)\n\nCheck the registry at root against SPEC.md and say what is wrong with it. errors are the ways it does not satisfy the format and warnings the ways it is unusual but allowed; summary is one line per record — its identifier, how many revisions and events it holds, and which revision is current. A registry is valid when errors is empty; nothing here writes.\n\n\n\n\n\n","title":"Archeion.validate"},{"category":"function","location":"api/#Archeion.reindex!","page":"API References","text":"reindex!(root) -> NamedTuple\n\nRewrite registry.toml's [projects] and [records] from the tree, leaving the rest of the file as it stands. This is what resolves a conflict in the index: take either side, run this, and the answer is the tree's rather than a hand-merged guess.\n\n\n\n\n\n","title":"Archeion.reindex!"},{"category":"function","location":"api/#Archeion.build","page":"API References","text":"build(root, out = joinpath(root, \"_site\")) -> (; out, records, bytes, dark)\n\nRender the registry at root as a static site in out, and say what was written: how many records, how many bytes, and dark, the tally of stylesheets that got a dark layer (dark, already, colourless, unknown — unknown is the count that is not supposed to be above zero, and each one is warned about by name — plus controls, the report pages that came away switchable).\n\nThe site is derived: out is replaced on every build, never committed, and every link in it is relative, so it reads from a sub-path, over SSH or from file://. Refuses a registry that does not validate, and refuses to replace an out it did not write. A revision is copied in as it is frozen; only the copy is ever touched (see dark.jl).\n\n\n\n\n\n","title":"Archeion.build"},{"category":"function","location":"api/#Archeion.check_settled","page":"API References","text":"check_settled(reg)\n\nRefuse to deposit into a registry whose own content is not committed. A deposit reads the tree to decide what it is adding to — the current revision it will name as parent, the index it will rewrite — so anything on disk that git does not have is something the next revision may cite and nobody else will ever see.\n\nThat is not hypothetical: a deposit interrupted between writing a revision and committing it leaves exactly that, the tree still validates, and the next deposit names the orphan as its parent and commits that. One git clean later the registry holds a committed revision whose parent resolves nowhere, and the parent's bytes are gone — not even as an unreferenced object.\n\n\n\n\n\n","title":"Archeion.check_settled"},{"category":"function","location":"api/#Archeion.sync!","page":"API References","text":"sync!(reg)\n\nBring the registry clone to its remote before anything is validated or written: a deposit checks the state it is about to add to, so that state must be the shared one. Refuses a registry whose own content is uncommitted (check_settled, the same rule deposit applies), and a history that is not a fast-forward of its remote (a rebase is the caller's call, not this function's). A clone with no remote, or a remote that cannot be reached, is left alone with a warning: depositing offline is normal.\n\n\n\n\n\n","title":"Archeion.sync!"},{"category":"function","location":"api/#Archeion.publish_revision!","page":"API References","text":"publish_revision!(reg, rev, title; remote = :pr) -> NamedTuple\n\nGet the commit deposit just made in reg to the shared repository. :pr pushes it on a branch of its own and opens a pull request with gh, :push pushes the current branch, rebasing onto the remote once if it moved and revalidating before it does, and :local leaves it alone. Returns what happened — pushed, the branch, and the pr URL when there is one.\n\n\n\n\n\n","title":"Archeion.publish_revision!"},{"category":"function","location":"api/#Archeion.check_source_published","page":"API References","text":"check_source_published(repo) -> Bool\n\nWhether repo's HEAD — the commit a revision deposited now names as the code that rendered it — is reachable from the remote's default branch. When it is not, the citation points at something only this clone has: a squash merge of the branch it sits on would replace it, and the revision would name a commit no one can resolve. Warns rather than refuses, because depositing while the work is still in review is a reasonable thing to do; merging that branch with a merge commit is what keeps the citation good.\n\n\n\n\n\n","title":"Archeion.check_source_published"},{"category":"function","location":"api/#Archeion.rebase_onto_remote!","page":"API References","text":"rebase_onto_remote!(reg)\n\nPut the deposit just committed in reg on top of whatever the remote gained meanwhile, and leave the registry validating. registry.toml is the one file every deposit writes, so two deposits made at once collide there — and because the index is derived, that collision has an answer that needs no judgement: take either side and regenerate it from the tree (§2.1). A conflict in anything else is a real one, so the rebase is abandoned and the registry is left as it was, for a person.\n\n\n\n\n\n","title":"Archeion.rebase_onto_remote!"},{"category":"function","location":"api/#Archeion.migrate!","page":"API References","text":"migrate!(root) -> NamedTuple\n\nConvert the registry/1 tree at root in place, and validate the result. Every identifier gets a UUID, which the files keep and the paths lose; every record moves to records/<year>/<slug>/; the index is written. Refuses a tree that is not registry/1, so a second run says so rather than making a mess.\n\nA conversion is not resumable: it renames directories one at a time, and a tree caught between the two formats is neither. So it is all or nothing, in two halves. Everything that can be known in advance — the identifiers, the slugs, the collisions among them, whether every project file and record.toml parses — is settled before the first rename. Whether the result validates can only be known afterwards, and if it does not the conversion is undone, which is why a registry under git must have nothing uncommitted before one starts: that clean tree is what makes undoing possible. A tree not under git has no undo, and is told so rather than left half converted in silence.\n\n\n\n\n\n","title":"Archeion.migrate!"},{"category":"function","location":"api/#Archeion.write_provenance!","page":"API References","text":"write_provenance!(revdir; reads, observations_dir, sources_dir, render_observation = nothing,\n                  allow_mismatch = false, source_contents = true) -> Dict\n\nWrite provenance.toml, provenance/points.tsv and the observations and source snapshots the points name into a revision being deposited. reads are records with the fields of POINT_COLUMNS (what Pinax.report returns). Refuses when a point's read bytes differ from the bytes its computation recorded, unless allow_mismatch, which is then recorded. An observation a point names that is not in observations_dir is listed as missing, never silently dropped.\n\n\n\n\n\n","title":"Archeion.write_provenance!"},{"category":"function","location":"api/#Archeion.provenance_from","page":"API References","text":"provenance_from(vault, report; allow_mismatch = false, source_contents = true) -> NamedTuple\n\nWhat deposit's provenance needs, from a DataVault vault and the result of Pinax.report: the points it read, where the vault keeps its observations and source snapshots, and the render observation. The method for a DataVault.Vault is defined when DataVault is loaded.\n\n\n\n\n\n","title":"Archeion.provenance_from"},{"category":"function","location":"api/#Archeion.restore","page":"API References","text":"restore(revdir, dest; token = nothing) -> NamedTuple\n\nLay out what revision revdir holds for the observation its points were computed under (or token): the study under dest/study, packages developed beside it under dest/dev/<name>, the packages it loaded from a depot under dest/depot/packages/<name>/<slug>, and the environment, with its Manifest's path entries pointed at the restored packages (the recorded bytes are kept beside it as Manifest.recorded.toml).\n\nNothing is resolved or downloaded. What the revision does not hold is listed in missing, and each depot package is checked against the tree its Manifest pins: trees maps its name to whether it matched. julia is the recorded binary when it is on this host and hashes as recorded.\n\n\n\n\n\n","title":"Archeion.restore"},{"category":"function","location":"api/#Archeion.verify","page":"API References","text":"verify(revdir; dest, entry = nothing, julia = nothing, event = true) -> NamedTuple\n\nrestore revdir into dest and run its study there — julia --project=<restored> <entry> with an empty depot of only the restored packages, an empty HOME, Pkg offline, the network namespace unshared where the host allows it, and the recorded Julia and BLAS thread counts. The study's DataVault output is redirected (DATAVAULT_OUTDIR) to dest/out, so a study must not pass outdir itself.\n\nEvery point the revision computed under that observation is then compared by the SHA-256 of its result file. When every one matches and event, a capability.verified event is written into the record's events/, saying what was compared and under which conditions — including whether the network was actually unavailable, since an offline flag alone does not show it.\n\n\n\n\n\n","title":"Archeion.verify"},{"category":"function","location":"api/#Archeion.git_tree_hash","page":"API References","text":"git_tree_hash(dir) -> String\n\nThe git tree id of dir — the value a Manifest's git-tree-sha1 pins a package to — computed from the files alone, so a restored package can be checked against its pin without git or Pkg.\n\n\n\n\n\n","title":"Archeion.git_tree_hash"},{"category":"function","location":"api/#Archeion.main","page":"API References","text":"main(args) -> exit code\n\nThe command line. usage() prints the same list; in short:\n\ninit [root] starts a registry — its directories, registry.toml, its workflows\nvalidate [root] prints the records and every warning and error, and returns 1 on any error\nbuild [root] [out] writes the site, by default to <root>/_site\npages [root] writes the workflows that publish it, pinned to this version\nreindex [root] rewrites registry.toml's index from the tree\nmigrate [root] converts a registry/1 tree, and prints which identifier became which UUID\nrestore <revision> <dest> lays out what a revision holds; verify <revision> <dest> also recomputes it there and, when every point matches, writes capability.verified\n\nReturns the process exit code: 0, 1 for a registry with errors, 2 for a usage problem.\n\n\n\n\n\n","title":"Archeion.main"},{"category":"module","location":"api/#Archeion.Archeion","page":"API References","text":"Archeion\n\nA registry of rendered research results, kept as plain files in a git repository and read with nothing but a text viewer if need be. The format is SPEC.md (spec = \"registry/2\"); this package is one implementation of it, depending on the standard library only.\n\nvalidate checks a registry against the format.\nbuild renders it as a static site with relative links only.\nnew_binding and deposit add a record, then revisions of it.\ndoc_fields (with Pinax loaded) takes what an entry needs from a rendered document.\nprovenance_from (with DataVault loaded) takes per-point provenance from a vault.\ninit starts a registry: its directories, its registry.toml, its workflows.\nsetup_pages writes the workflows that publish the catalogue as a site.\n\nFrom a shell: julia -m Archeion init [root], validate [root], build [root] [out], pages [root] [--branch=B] [--runner=R] [--site=DIR], reindex [root], migrate [root].\n\n\n\n\n\n","title":"Archeion.Archeion"},{"category":"section","location":"guide/registry/#Create-a-registry","page":"Create a registry","text":"A registry is a directory tree. Making one does not involve git, GitHub, or a server — those come in later, and only if you want them, depending on how the registry will be read.","title":"Create a registry"},{"category":"section","location":"guide/registry/#1.-The-tree","page":"Create a registry","text":"using Archeion\nArcheion.init(\"path/to/registry\"; title = \"The Registry\", tagline = \"one question per record\")\n\nThat writes registry.toml, empty projects/ and records/, a .gitignore for the parts that are derived, and — unless you pass pages = false — the workflows in step 3.\n\nregistry.toml is the one file that is about you rather than about the format. Its [site] block is what a reader meets first:\n\n[site]\ntitle = \"The Registry\"\ntagline = \"one question per record\"\nfooter = \"\"\n\n# appearance = \"dark\"              # \"system\" (default) | \"light\" | \"dark\"\n\n# [[site.links]]\n# text = \"The lab\"\n# url = \"https://example.org\"\n\nEdit it now rather than later. Everything else in the file is generated.","title":"1. The tree"},{"category":"section","location":"guide/registry/#2.-A-project","page":"Create a registry","text":"Records belong to projects, and projects are written by hand — naming a line of work is a decision, not a side effect. Create projects/<slug>.toml:\n\nspec = \"registry/2\"\nuuid = \"247b870f-4313-4ae6-aa32-5d309fe806e1\"\nname = \"Chaotic attractors\"\ncreated = 2026-09-24T12:40:00Z\n\nThe UUID is yours to generate:\n\n$ julia -e 'using UUIDs; println(uuid4())'\n\nThen bring the index up to date and check the result:\n\nArcheion.reindex!(\"path/to/registry\")\nArcheion.validate(\"path/to/registry\")     # -> (; errors, warnings, summary)\n\nvalidate reports everything that is wrong at once, and when the index has fallen behind the tree it names the command that fixes it. You will use it constantly; it is cheap and it never writes.","title":"2. A project"},{"category":"section","location":"guide/registry/#3.-How-will-it-be-read?","page":"Create a registry","text":"This is the decision that determines whether you need anything beyond the directory. Three answers, and the registry itself is identical in all three:\n\n what it needs when\nyou, on this machine nothing further while the work is still yours\npeople you can reach over SSH a machine that rebuilds the site into a directory a private registry, read through Tailscale or an SSH file browser\nanyone with the URL a GitHub repository and Pages a public registry\n\nThe rest of this page is the third one, because it needs the most setup. For the second, skip to the private variant. For the first, there is nothing to do: build writes _site/, every link in it is relative, and a browser opens it from file://.","title":"3. How will it be read?"},{"category":"section","location":"guide/registry/#4.-The-CI-(if-it-lives-on-GitHub)","page":"Create a registry","text":"setup_pages writes the workflows. init has already called it unless you asked it not to, and you can call it again to refresh them after upgrading Archeion:\n\nArcheion.setup_pages(\"path/to/registry\")   # -> (; written, skipped)\n\nworkflow when what it does\nvalidate.yml every pull request and push runs validate and build, so a broken registry cannot merge\npages.yml push to the default branch builds the site and publishes it to GitHub Pages\nsite.yml in place of pages.yml, when you pass site builds into a directory on your own runner, to be read from there over SSH\n\nThe Archeion version is pinned into the workflows — to the version that wrote them — and that is the version registry.toml's implementation field should name. Re-run setup_pages after an upgrade so the two agree; written and skipped tell you which files it touched.","title":"4. The CI (if it lives on GitHub)"},{"category":"section","location":"guide/registry/#A-registry-that-must-not-be-published","page":"Create a registry","text":"On every plan but Enterprise Cloud, GitHub Pages serves a site to anyone with the URL whether or not the repository is private. So for a registry that must not be readable, do not publish it:\n\nArcheion.setup_pages(\"path/to/registry\";\n                     runner = \"self-hosted\",\n                     site   = \"/srv/registry-site\")\n\nThat writes site.yml instead of pages.yml. The site is rebuilt into that directory on the machine runner names, on every push, and read from there over SSH — through Tailscale, an SSH file browser, or any other way you already reach that filesystem. Nothing is published and no URL exists to leak.\n\nIt is also a real http origin rather than file://, which matters for anything in a report that a browser treats as cross-origin.","title":"A registry that must not be published"},{"category":"section","location":"guide/registry/#5.-Where-git-comes-in","page":"Create a registry","text":"Depositing is the one thing that needs it. deposit commits each revision as it lands, so the registry must be a git repository by the time you publish a result into it — and so must the repository that renders the report, because a revision cites the commit that produced it.\n\nMeasured, on a registry with no .git at all:\n\n \ninit, reindex!, validate, build all work\ndeposit fails, at git add, after the revision is already on disk\n\nSo git init before your first deposit, not after:\n\n$ git init && git add -A && git commit -m \"the registry\"\n\nAnd keep committing. deposit and sync! refuse a registry with uncommitted content of its own, because a half-written state is what the next deposit would build on; migrate! is stricter still and refuses any uncommitted change at all, because its undo is git checkout.","title":"5. Where git comes in"},{"category":"section","location":"guide/registry/#Then","page":"Create a registry","text":"Publish a result into it.","title":"Then"},{"category":"section","location":"guide/everything-else/#Everything-else","page":"Everything else","text":"The rest of what Archeion does, in roughly the order you are likely to need it.","title":"Everything else"},{"category":"section","location":"guide/everything-else/#From-the-shell,-without-writing-Julia","page":"Everything else","text":"Every routine operation has a command. This is what the CI workflows use, and usually what you want from a terminal:\n\n$ julia --project=archeion-env -m Archeion validate .\n$ julia --project=archeion-env -m Archeion build .\n\ncommand what it does\ninit [root] starts a registry — its directories, registry.toml, its workflows\nvalidate [root] prints the records and every warning and error\nbuild [root] [out] writes the site, by default to <root>/_site\npages [root] writes the workflows that publish it, pinned to this version\nreindex [root] rewrites registry.toml's index from the tree\nmigrate [root] converts a registry/1 tree, printing which identifier became which UUID\n\nThe exit code is 0, 1 for a registry with errors, and 2 for a usage problem — which is what makes validate a CI gate with no wrapper around it.","title":"From the shell, without writing Julia"},{"category":"section","location":"guide/everything-else/#Checking-a-registry","page":"Everything else","text":"r = Archeion.validate(\"path/to/registry\")\nr.errors      # a registry with any of these is not valid\nr.warnings    # legal, but probably not what was meant\nr.summary     # one line per record: revisions, events, which one is current\n\nIt never writes, so run it whenever you are unsure. If the index has fallen behind the tree — after editing a project file by hand, say — it says so and names the fix:\n\nArcheion.reindex!(\"path/to/registry\")\n\nreindex! regenerates registry.toml's [projects] and [records] from the tree and leaves the rest of the file alone. It is also how you settle a merge conflict in the index: take either side, run it, and the answer is the tree's rather than a hand-merged guess.","title":"Checking a registry"},{"category":"section","location":"guide/everything-else/#Working-with-a-shared-registry","page":"Everything else","text":"Three things happen around a deposit once more than one person is depositing.\n\nsync! brings your clone to its remote before anything is validated or written. A deposit checks the state it is about to add to, so that state has to be the shared one. It refuses a registry with uncommitted content of its own rather than fast-forwarding over your work.\n\ncheck_source_published asks whether the commit that rendered the report is reachable from its remote's default branch. If it is not, the revision will cite code nobody else can see. That is a warning rather than a refusal: there are legitimate reasons to deposit before pushing.\n\nrebase_onto_remote! handles the one file every deposit touches. Two deposits made at the same time both write registry.toml, so they conflict there and nowhere else; this puts yours on top of what arrived meanwhile and regenerates the index instead of asking you to merge it.\n\npublish calls the first two every time. The third is reached only on the remote = :push path, and only when the first push is rejected because the remote moved — on the default :pr path the commit goes to a fresh branch and nothing is ever rebased. You need any of them by name only when driving deposit directly.","title":"Working with a shared registry"},{"category":"section","location":"guide/everything-else/#Converting-an-old-registry","page":"Everything else","text":"Archeion.migrate!(\"path/to/registry\")   # -> (; projects, records, summary, ids, at)\n\nFor registry/1 trees. Commit first: the conversion is all-or-nothing when it can be, and the way it puts your tree back is git's, so it needs a clean working tree to start from.\n\nA tree not under git has no undo, and the failure mode is worth knowing before you meet it: the conversion runs to the end, and if the converted tree does not validate it is left as it is — part registry/1 and part registry/2, to be repaired by hand. The error says so rather than leaving you to find out.\n\nids is the part you cannot reconstruct afterwards — which old identifier became which UUID. Bindings live in other repositories, so nothing here can update them for you, and you need that table to do it yourself.","title":"Converting an old registry"},{"category":"section","location":"guide/everything-else/#Provenance","page":"Everything else","text":"With DataVault loaded, a deposit can record where each point of data came from, not only which code produced the report. write_provenance! and provenance_from are that path, and publish uses them when it is given a vault.\n\nprovenance.toml is served with the site. The evidence behind it — the point tables, the source snapshots under repro/ — stays in the repository and is deliberately not copied into _site: a table of 80,000 points is megabytes per revision, and a site that carried them would carry them once for every revision ever deposited.","title":"Provenance"},{"category":"section","location":"guide/everything-else/#The-site's-appearance","page":"Everything else","text":"The catalogue and the reports in it are read as one thing, so they share a palette and one colour-scheme control. registry.toml sets what a reader gets before choosing:\n\n[site]\nappearance = \"dark\"       # \"system\" (default) | \"light\" | \"dark\"\n\nA reader's own choice is remembered in their browser and outranks it. Reports frozen before any of this existed get a dark layer and the control in the site's copy — the revision itself is never touched. A report whose stylesheet uses a colour Archeion does not recognise keeps its light one instead of going half dark, and build counts those for you.","title":"The site's appearance"},{"category":"section","location":"guide/everything-else/#Where-the-details-are","page":"Everything else","text":"Everything above has a docstring saying what each argument means, what comes back, and what it refuses to do. The API References list them grouped by when you need them.","title":"Where the details are"},{"category":"section","location":"architecture/#Architecture","page":"Architecture","text":"The format says what a registry is. This says why it is that, and what each decision costs — the part that is otherwise legible only by reading the source.","title":"Architecture"},{"category":"section","location":"architecture/#The-format-is-not-the-implementation","page":"Architecture","text":"SPEC.md is normative; Archeion.jl is one implementation of it. The split is load-bearing rather than tidy: a registry has to outlive the tool that wrote it, so the tool must not be where the rules live.\n\nConcretely, validate, build and the index never invoke git, and nothing in the format requires it — a registry read off a disk, over SSH, or out of a backup is the same registry. Only the writers use git, and for the two things git is good at: making a deposit a commit, and making a failed conversion undoable.","title":"The format is not the implementation"},{"category":"section","location":"architecture/#Identity-is-inside-the-file;-the-path-is-a-name","page":"Architecture","text":"Every project and record carries a UUID in its own TOML, and its directory is named by a slug. So:\n\na record can be renamed without breaking anything that referred to it\ntwo registries may use the same slug for different things\nthe index can be rebuilt from the tree, because identity travels with the content\n\nThe cost lands in one place. A binding names a record by UUID but a registry by relative path, and bindings live in the repositories that render reports — which a registry cannot reach. That is why migrate! returns its ids table instead of fixing bindings itself, and why the unit of retirement has to be a whole registry rather than a record (SPEC.md §10).","title":"Identity is inside the file; the path is a name"},{"category":"section","location":"architecture/#A-revision-is-frozen,-so-withdrawal-is-an-addition","page":"Architecture","text":"Every file of a revision is covered by its SHA256SUMS, entry.toml included. A digest somebody cited keeps meaning what it meant, and validate refuses a registry where one no longer matches.\n\nThat forbids editing, so everything that would have been an edit becomes an addition:\n\n \ncorrecting a new revision, naming the old one as parent\nwithdrawing a yank event\nreplacing out of line a supersede event\n\nWhich revision is current is then a computation over that history rather than a field somebody sets (SPEC.md §7.1) — and it has three outcomes, including \"in conflict\", because two people depositing from the same parent is a real state and resolving it by timestamp would be a guess.","title":"A revision is frozen, so withdrawal is an addition"},{"category":"section","location":"architecture/#The-index-is-derived,-and-committed-anyway","page":"Architecture","text":"registry.toml's [projects] and [records] can be rebuilt by walking the tree. They are committed regardless, because a reader resolving one UUID should not have to walk anything, and a registry should be able to say what it holds without a tool.\n\nBeing derivable is what makes that cheap: a validator checks it against the tree, and a writer that meets a conflict in it — it is the one file every deposit touches — regenerates it instead of merging by hand. reindex! is that operation, and it is why two deposits can collide without anybody having to resolve TOML.","title":"The index is derived, and committed anyway"},{"category":"section","location":"architecture/#The-site-is-derived,-so-the-copy-can-carry-what-the-original-cannot","page":"Architecture","text":"build rewrites _site from scratch every run, and it is never committed. That is not only hygiene. Because the site is a copy, it can hold what a frozen revision may not: a report published before this package had a dark mode gets a derived dark layer and a colour-scheme control in the site's copy, while the revision stays byte-identical under its own checksums. (Unless its stylesheet draws with a colour Archeion does not recognise: half a conversion is worse than none, so that report keeps its light one and build reports how many there are.)\n\nThe general rule — anything a reader should get but a revision must not promise belongs in the derived layer.","title":"The site is derived, so the copy can carry what the original cannot"},{"category":"section","location":"architecture/#Writers-refuse;-they-do-not-repair","page":"Architecture","text":"deposit and sync! demand a registry with no uncommitted content of its own, because a half-written state is what the next deposit would build on. migrate! is stricter — any uncommitted change at all — because its undo is git checkout. Beyond that:\n\ndeposit stages under _incoming/ and writes SHA256SUMS last, so the presence of that file means the revision is complete; if the result does not validate, the revision is taken back out\nmigrate! settles everything knowable — identifiers, slugs, collisions among them, whether every project file and record.toml parses — before the first rename, and undoes the rest with git if the converted tree does not validate\n\nThe limit is worth knowing rather than discovering: the undo covers the checks, not git itself. A deposit into a registry that is not a git repository fails at git add, after the revision has been moved into place.","title":"Writers refuse; they do not repair"},{"category":"section","location":"architecture/#Two-faces,-one-document","page":"Architecture","text":"A deposit takes a gallery — what a person reads — and an agent view, what a program reads, both rendered from the same Pinax document. Neither is derived from the other and neither is the authority: they are two renderings of one source, so a machine reader never has to parse HTML and a human reader never has to read JSON.","title":"Two faces, one document"},{"category":"section","location":"architecture/#Only-the-standard-library","page":"Architecture","text":"Archeion depends on Dates, Random, SHA, TOML and UUIDs, and nothing else. Pinax and DataVault are reached through package extensions, so validate and build run on a machine that has neither — which is what makes a registry's CI cheap, and what keeps the library usable from a server that will never render a figure.\n\npublish exists only when both are loaded. That is the seam: the parts needing a plotting stack are the parts that are optional.","title":"Only the standard library"},{"category":"section","location":"architecture/#Where-the-reasoning-lives","page":"Architecture","text":"This page is a summary. The argument for any particular decision is in the source, beside the code it constrains — the comments in src/ run to some four hundred lines, written to be read.","title":"Where the reasoning lives"},{"category":"section","location":"guide/publish/#Publish-a-result","page":"Publish a result","text":"Two steps. The first happens once, when a record is born; the second happens every time you have a new answer to the same question.","title":"Publish a result"},{"category":"section","location":"guide/publish/#Once-—-name-the-record","page":"Publish a result","text":"using Archeion\n\nArcheion.new_binding(\".registry/bindings/henon.toml\";\n                     root    = \"../path/to/registry\",\n                     project = \"247b870f-4313-4ae6-aa32-5d309fe806e1\",\n                     slug    = \"henon-correlation-dimension\")\n\nThis writes a binding, and the binding lives in the repository that renders your report — not in the registry:\n\nspec = \"registry/2\"\nregistry = \"../path/to/registry\"                   # relative to this file\nproject = \"247b870f-4313-4ae6-aa32-5d309fe806e1\"\nrecord = \"9c53a959-6d1e-44d4-8e7e-d24cd8d402c7\"    # minted here, for you\nslug = \"henon-correlation-dimension\"\nkind = \"report\"\n\nCommit it. It is the only thing that says which record your script writes to, and you will not edit it again. new_binding refuses to overwrite an existing file, so copying a script cannot silently make it continue somebody else's record.\n\nwarning: The slug is not checked for collisions here\nnew_binding checks the slug's shape, not whether it is free. A name already used by another record of the same year is accepted here and refused at your first deposit:…/records/2026/henon-correlation-dimension exists: another record of this\nyear is already called henon-correlation-dimension; give this one another slugYour registry is untouched, but you are left holding a committed binding that points at nothing. Delete it and make another. (It cannot be checked earlier: a record is filed under the year it is frozen, which is not known when the binding is written.)","title":"Once — name the record"},{"category":"section","location":"guide/publish/#Every-time-—-deposit","page":"Publish a result","text":"using Archeion, Pinax\n\n# … build the document with @page / @section / @figure …\n\nPinax.render(; out = joinpath(OUT, \"gallery\"))\nPinax.render(; out = joinpath(OUT, \"agent\"), theme = :agent)\n\nr = deposit(BINDING;\n            gallery     = joinpath(OUT, \"gallery\"),\n            agent       = joinpath(OUT, \"agent\"),\n            source_repo = @__DIR__,\n            doc         = Archeion.doc_fields(Pinax.current_document();\n                                              tags = [\"chaos\", \"dimension\"]))\n\nTwo faces go in: the gallery a person reads and the agent view a program reads. Pinax renders both from the same document.\n\nWhat comes back names what happened:\n\nr.record    # \"9c53a959-6d1e-44d4-8e7e-d24cd8d402c7\"\nr.rev       # \"20260924T130223Z-axz9\"\nr.parents   # the revision this one answers after\nr.dir       # where it landed\nr.commit    # the commit that added it\nr.pushed    # whether it reached the remote\nr.dirty     # whether the rendering repository had uncommitted changes\n\nThe first revision and the tenth are the same call. deposit looks up the binding's record UUID in the registry and works out for itself whether it is creating the record or adding to it. Parents default to the record's current head, so re-running your script links the new answer to the one before it without being told.\n\nIf the revision turns out not to validate, it is taken back out and the index rewritten — you are left where you started.\n\nnote: What `deposit` needs from git\nIt commits each revision as it lands, so both repositories must be git repositories: the registry, and the one rendering the report (with at least one commit, because a revision cites the commit that produced it).The undo covers the checks. It does not cover git itself: measured on a registry with no .git, deposit fails at git add — after the revision has been moved into place. The revision is complete and valid, it simply is not committed. If a deposit reports a git error, look at the registry before re-running it.","title":"Every time — deposit"},{"category":"section","location":"guide/publish/#One-call-instead-of-two","page":"Publish a result","text":"With Pinax and DataVault loaded, publish is the whole path:\n\nArcheion.publish(vault, recipe;\n                 binding     = BINDING,\n                 title       = \"The correlation dimension of the Hénon attractor\",\n                 out         = OUT,\n                 status      = :trial,\n                 source_repo = @__DIR__,\n                 remote      = :pr)\n\nIt syncs the registry with its remote, warns you if the commit that rendered the report has not been pushed, renders both faces, deposits, and then — depending on remote — opens a pull request (:pr), pushes to the branch (:push), or leaves the commit local (:local).\n\nstatus is not optional here, and the two values mean different things to a reader: :trial is an answer you are still arguing with, :final is one you are prepared to be held to.","title":"One call instead of two"},{"category":"section","location":"guide/publish/#Publish-the-catalogue","page":"Publish a result","text":"Archeion.build(\"path/to/registry\")   # -> _site/\n\n_site is rebuilt from scratch every time and never committed. In practice you will rarely run this by hand — pages.yml does it on every push.","title":"Publish the catalogue"},{"category":"section","location":"guide/publish/#A-worked-example","page":"Publish a result","text":"The demonstration repository has both shapes:\n\nscripts/build.jl — Pinax and deposit directly\nscripts/lorenz.jl — publish from a DataVault vault\n\nwith their bindings committed beside them, under .registry/bindings/.","title":"A worked example"},{"category":"section","location":"guide/publish/#Then","page":"Publish a result","text":"Something was wrong with it? Correct or withdraw a result.","title":"Then"},{"category":"section","location":"guide/correct/#Correct-or-withdraw-a-result","page":"Correct or withdraw","text":"Start with the part that surprises people:\n\ndanger: A revision is never edited and never deleted\nOnce deposited, a revision's files are covered by its own SHA256SUMS. Changing one breaks a checksum somebody may already have cited, and validate will refuse the registry until you put it back. This is not a rail you can switch off — it is the only reason a digest in a paper keeps meaning what it meant.\n\nSo \"editing\" and \"deleting\" happen a different way: you add something that changes what the record currently says.\n\nyou want to you do\nfix a mistake, add data, redo the analysis deposit a new revision\nsay an answer was wrong and should not be used write a yank event\nsay one revision replaces another that is not its parent write a supersede event\nleave a remark on a record or a revision write a comment event\nremove a record entirely see below","title":"Correct or withdraw a result"},{"category":"section","location":"guide/correct/#Correcting:-deposit-again","page":"Correct or withdraw","text":"The ordinary case needs nothing special. Re-run your script:\n\ndeposit(BINDING; gallery = …, agent = …, source_repo = @__DIR__, doc = …)\n\nThe new revision names the old one as its parent, and the record's current revision becomes the new one. The old revision stays where it is, still readable, still checksummed — which is the point. A reader who followed a link to it last year still lands on what they read.","title":"Correcting: deposit again"},{"category":"section","location":"guide/correct/#Withdrawing:-a-yank-event","page":"Correct or withdraw","text":"There is no API for this. Events are written by hand into the record's events/ directory; deposit only reads them, to work out which revision is current. The file name and the fields are fixed by the format:\n\nrecords/2026/henon-correlation-dimension/events/20260925T101500Z-loc-a7f3.toml\n\nspec = \"registry/2\"\nkind = \"yank\"\nat = 2026-09-25T10:15:00Z\nreason = \"the scaling region was chosen after seeing the answer\"\n\n[subject]\nrecord = \"9c53a959-6d1e-44d4-8e7e-d24cd8d402c7\"\nrev = \"20260924T130223Z-axz9\"\n\nThe name is <YYYYMMDDTHHMMSSZ>-<origin>-<key>.toml; use loc as the origin for an event you write yourself, and four random lowercase characters as the key. Required: spec, kind, at, and subject.record, which must be this record's UUID. subject.rev narrows it to one revision. You may leave it out to speak about the record as a whole, but note that a yank without a subject.rev does not withdraw the record: which revision is current is computed from the yanks that name one, so a record-level yank is a remark, not a retraction.\n\nThen check it:\n\nArcheion.validate(\"path/to/registry\")\n\nvalidate checks the file name, the kind, and the subject. A subject.rev or subject.anchor that does not exist is a warning — an event may legitimately outlive what it talks about — but a subject.record that is not this record's UUID is an error.\n\nThe four kinds registry/2 knows are comment, yank, supersede and capability.verified. An unknown kind is also a warning — a reader counts and shows it rather than dropping it — so a later version of the format can add kinds without invalidating your registry.","title":"Withdrawing: a yank event"},{"category":"section","location":"guide/correct/#What-a-yank-does-to-\"current\"","page":"Correct or withdraw","text":"live   = revisions not named by a yank event\nheads  = live revisions that are not a parent of another live revision,\n         and are not superseded by a live revision\n\nexactly one head → that is the current revision\nno heads → the record is withdrawn. It stays in the registry and its history stays readable, but nothing is current\nmore than one head → the record is in conflict, and nothing is chosen by date. You resolve it with a supersede event, or with a new revision naming both heads as parents\n\nThat last case is the one to watch: it is what happens when two people deposit from the same parent.","title":"What a yank does to \"current\""},{"category":"section","location":"guide/correct/#Superseding","page":"Correct or withdraw","text":"The same shape, with kind = \"supersede\", and the subject is the revision being replaced:\n\nspec = \"registry/2\"\nkind = \"supersede\"\nat = 2026-09-25T10:20:00Z\n\n[subject]\nrecord = \"9c53a959-6d1e-44d4-8e7e-d24cd8d402c7\"\nrev = \"20260922T060556Z-t8c9\"\n\nUse it when the replacement is not a descendant — a fresh analysis rather than a revision of the old one. If the new revision is a child of the old, its parents already says so and no event is needed.","title":"Superseding"},{"category":"section","location":"guide/correct/#Events-are-also-never-edited","page":"Correct or withdraw","text":"Changed your mind about a yank? Write another event. A record's history is the sequence of what was said, not the last thing said.","title":"Events are also never edited"},{"category":"section","location":"guide/correct/#Removing-a-record","page":"Correct or withdraw","text":"There is no supported way to remove a record, and that is deliberate: the point of depositing something is that the link keeps working.\n\nIf you truly must — a record deposited into the wrong registry, or one containing something that should never have been published — it is a git operation, not an Archeion one: delete the directory, run reindex!, run validate, commit. Be clear about what you are accepting:\n\nanyone who cloned or forked the registry still has it\nany link or digest that named it now points at nothing\nits events go with it, so the reason it was withdrawn is gone too\n\nvalidate will not warn you about the loss. Parents are only ever checked within one record, so removing a whole record leaves nothing dangling; the single complaint is that the index no longer matches the tree, which reindex! silently settles. Deleting one revision out of a record is the case validate does catch, because a sibling names it as a parent.\n\nA yank is almost always the honest answer instead. It says this was here and should not be used, which is true; deletion says nothing at all.","title":"Removing a record"},{"category":"section","location":"#Archeion","page":"Archeion","text":"Archeion is the registry manager for Pinax.\n\nPinax renders one study into a self-contained report. Archeion is where those reports go: a registry that keeps them, keeps them findable, and keeps the old ones exactly as they were when you published them.\n\nA registry is a directory tree — one directory per record, one frozen directory per revision. A result you can only open through a service is a result that expires when the service does, so nothing has to be running for this one to be read, and nothing has to be installed to read it: no database, no server, and no git.\n\nArcheion's writers do use git, so that a deposit is a commit and a failed conversion can be undone. Reading is a different matter — validate, build and the index never touch it, and a reader needs neither Archeion nor git at all.\n\nThat is what lets a registry be read wherever it happens to live:\n\n \na published site GitHub Pages, from pages.yml\na private machine, over SSH built into a directory on your own runner and read from there — through Tailscale, an SSH file browser, or anything else that reaches the filesystem\nthe files themselves ls, an editor, sha256sum -c, a browser on file://\n\nEvery link the site generates is relative, so the last two work without a web server.\n\ntip: A demonstration repository\narcheion-demo is a small public registry, built by Archeion on every push. It is the quickest way to see what this produces.The site it builds — the page you land on is the catalogue; the logistic map record is a record with a history behind it, and its current revision is one frozen answer.Worth reading in the other order, though: the files first, then the site. The files are the registry; the site is made from them.","title":"Archeion"},{"category":"section","location":"#What-you-get","page":"Archeion","text":" \na record one question you keep coming back to\na revision one answer to it, frozen — checksummed, never edited, naming the answer it came after\na catalogue a static site built from the tree, every link relative, readable over file://\n\nA record accumulates revisions instead of being overwritten, so \"what did we think in September\" stays answerable. A revision is covered by its own SHA256SUMS, so a digest you cited in a paper keeps meaning what it meant.\n\nNames live in the paths, identity lives in the files: every record and project carries a UUID, and the directory is named by a slug. That is why renaming a record breaks nothing, and why two registries can use the same slug for different things.\n\nThe exact rules are in the format. You do not need them to use this — they are there so that somebody who finds your registry in ten years does not need this package either.","title":"What you get"},{"category":"section","location":"#How-to-use-it","page":"Archeion","text":"The step-by-step is its own section, in the order you meet it:\n\n \nCreate a registry the tree, a project, and the CI that keeps it honest\nPublish a result bind a record once, then deposit a revision each time\nCorrect or withdraw what to do when an answer was wrong — revisions are never edited\nEverything else the command line, shared registries, migration, provenance\n\nWhy it is built the way it is — frozen revisions, a derived index, bindings in someone else's repository — is Architecture. The rules themselves are the format.\n\nIn short: init once for the registry, new_binding once per record, and deposit every time you have a new answer. The first revision and the tenth are the same call.","title":"How to use it"},{"category":"section","location":"#Reading-a-registry-without-Archeion,-or-git,-or-a-web-server","page":"Archeion","text":"Open registry.toml: it lists every project and record by UUID, with a path. Follow the path. Each record says what it is in record.toml; each revision holds its report, an entry.toml, a README.md and a SHA256SUMS. sha256sum -c SHA256SUMS verifies one on any machine with coreutils.\n\nNone of that needs this package, and none of it needs the repository to be a git repository either — a copy on a disk, or a directory you reach over SSH, is the same registry.\n\nThe demonstration repository's tree is the shortest way to check that this is true.","title":"Reading a registry without Archeion, or git, or a web server"}]
}
